'use strict';

var _createClass = function () { function defineProperties(target, props) { for (var i = 0; i < props.length; i++) { var descriptor = props[i]; descriptor.enumerable = descriptor.enumerable || false; descriptor.configurable = true; if ("value" in descriptor) descriptor.writable = true; Object.defineProperty(target, descriptor.key, descriptor); } } return function (Constructor, protoProps, staticProps) { if (protoProps) defineProperties(Constructor.prototype, protoProps); if (staticProps) defineProperties(Constructor, staticProps); return Constructor; }; }();

function _classCallCheck(instance, Constructor) { if (!(instance instanceof Constructor)) { throw new TypeError("Cannot call a class as a function"); } }

function _possibleConstructorReturn(self, call) { if (!self) { throw new ReferenceError("this hasn't been initialised - super() hasn't been called"); } return call && (typeof call === "object" || typeof call === "function") ? call : self; }

function _inherits(subClass, superClass) { if (typeof superClass !== "function" && superClass !== null) { throw new TypeError("Super expression must either be null or a function, not " + typeof superClass); } subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: { value: subClass, enumerable: false, writable: true, configurable: true } }); if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass; }

var BleService = require('./base');
var util = require('../util/util');

var UUID_SERVICE_SCRATCH = util.normalizeUUID('a495ff20c5b14b44b5121370f02d74de');
var UUID_CHAR_SCRATCH_BANK_1 = util.normalizeUUID('a495ff21c5b14b44b5121370f02d74de');
var UUID_CHAR_SCRATCH_BANK_2 = util.normalizeUUID('a495ff22c5b14b44b5121370f02d74de');
var UUID_CHAR_SCRATCH_BANK_3 = util.normalizeUUID('a495ff23c5b14b44b5121370f02d74de');
var UUID_CHAR_SCRATCH_BANK_4 = util.normalizeUUID('a495ff24c5b14b44b5121370f02d74de');
var UUID_CHAR_SCRATCH_BANK_5 = util.normalizeUUID('a495ff25c5b14b44b5121370f02d74de');

var BANK_MAP = {
  1: UUID_CHAR_SCRATCH_BANK_1,
  2: UUID_CHAR_SCRATCH_BANK_2,
  3: UUID_CHAR_SCRATCH_BANK_3,
  4: UUID_CHAR_SCRATCH_BANK_4,
  5: UUID_CHAR_SCRATCH_BANK_5
};

/**
 * LightBlue Scratch Service
 */

var ScratchService = function (_BleService) {
  _inherits(ScratchService, _BleService);

  function ScratchService() {
    _classCallCheck(this, ScratchService);

    return _possibleConstructorReturn(this, (ScratchService.__proto__ || Object.getPrototypeOf(ScratchService)).apply(this, arguments));
  }

  _createClass(ScratchService, [{
    key: 'readScratch',
    value: function readScratch(bank, callback) {
      var bankCharUUID = BANK_MAP[bank];
      if (bankCharUUID) {
        this._performCachedLookup(bankCharUUID, function (err, buf) {
          callback(err, buf);
        });
      } else {
        callback('Invalid bank: ' + bank);
      }
    }
  }, {
    key: 'writeScratch',
    value: function writeScratch(bank, data, callback) {
      var bankCharUUID = BANK_MAP[bank];
      if (bankCharUUID) {
        this._characteristics[bankCharUUID].write(data, true, function (err) {
          setTimeout(callback, 50, err);
        });
      } else {
        callback('Invalid bank: ' + bank);
      }
    }
  }]);

  return ScratchService;
}(BleService);

module.exports = {
  ScratchService: ScratchService,
  UUID: UUID_SERVICE_SCRATCH
};